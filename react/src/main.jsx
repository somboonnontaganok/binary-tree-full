import React from 'react'
import ReactDOM from 'react-dom/client'
import LoginNew from './components/LoginPage/LoginNew'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <LoginNew />
  </React.StrictMode>,
)
